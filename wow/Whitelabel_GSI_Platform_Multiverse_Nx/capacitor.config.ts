import type { CapacitorConfig } from "@capacitor/cli"

const config: CapacitorConfig = {
  appId: "com.r001.anip",
  appName: "multiverse_nx_work",
  webDir: "dist/apps/r001/.output/public" // 指向這裡
}

export default config
