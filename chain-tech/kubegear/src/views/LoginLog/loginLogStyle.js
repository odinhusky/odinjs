const logingLogStyle = (theme) => {
  // 取出斷點
  // const { laptop } = theme.bp;

  return {
    ctrlBarDatePicker: {
      width: 200,
      backgroundColor: theme.palette.trans
    }
  }
}

export default logingLogStyle;