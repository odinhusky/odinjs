import { IMyModifyModalProps } from './types';

export const MyModifyModal = (props: IMyModifyModalProps) => {
  return null;
};

export const showMyModifyModal = (props: IMyModifyModalProps) => {
  return null;
};

export default showMyModifyModal;
