import { BaseModalCloseButtonProps } from './BaseModalCloseButtonProps';

export const CloseBtnUnit = (props: BaseModalCloseButtonProps) => {
  return null;
};

export const BaseModalCloseButton = (props: BaseModalCloseButtonProps) => {
  return null;
};

export default BaseModalCloseButton;
