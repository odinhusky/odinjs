import { CustomerServiceListProps } from './CustomerServiceListProps';

export const CustomerServiceList = (props: CustomerServiceListProps) => {
  return null;
};

export default CustomerServiceList;
