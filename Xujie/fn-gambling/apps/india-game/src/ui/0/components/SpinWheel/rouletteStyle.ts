import { cx } from '@libs/commonUtils';

export const rechargeRouletteClass = cx(
  'max-w-[318px] max-h-[318px]',
  'phone:max-w-[404px] phone:max-h-[404px]',
  'w-full h-full'
);

export const inviteRouletteClass = cx('w-full h-full');
