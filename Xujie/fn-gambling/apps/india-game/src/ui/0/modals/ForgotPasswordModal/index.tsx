// 再次獲取驗證碼需等待秒數
const OTP_CODE_COUNTDOWN = 120;

const CloseButton = ({
  onClose,
  customClass,
}: {
  onClose: () => void;
  customClass?: string;
}) => {
  return null;
};

/** 忘記密碼modal */
export const ForgotPasswordModal = () => {
  return null;
};
