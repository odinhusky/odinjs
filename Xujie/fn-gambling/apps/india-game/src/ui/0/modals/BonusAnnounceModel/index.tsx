import 'swiper/css';
import 'swiper/css/pagination';
import { ParsingAnnouncementResult } from '@mode2/usecase/announcement/useParsingAnnouncementsContent';

const CloseButton = ({
  customClass,
  closePop,
}: {
  customClass?: string;
  closePop?: () => void;
}) => {
  return null;
};

interface BonusBannerImageProps extends ParsingAnnouncementResult {
  closePop?: () => void;
}

const BonusBannerImage = (props: BonusBannerImageProps) => {
  return null;
};

/** 首頁獎勵公告modal */
export const BonusAnnounceModel = () => {
  return null;
};
