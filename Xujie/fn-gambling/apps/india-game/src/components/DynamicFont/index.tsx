/**
 * @deprecated 直接使用 Vite 占位符 動態改寫 index.html
 * @constructor
 */
export const DynamicFont = () => {
  // const { VITE_GLOBAL_FONT_FAMILY, VITE_GLOBAL_FONT_FAMILY_LINK } = import.meta.env;

  return <></>;
  // return VITE_GLOBAL_FONT_FAMILY ? (
  //   <link rel="stylesheet" href={`${VITE_GLOBAL_FONT_FAMILY}`} />
  // ) : null;
};
